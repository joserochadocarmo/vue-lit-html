import {Navbar, PageHeading, StaffList} from './components/utils';

//Root Instance
new Vue({
    el: '#app',
    data: {},
    components: {
        Navbar,
        PageHeading,
        StaffList
      }
})
